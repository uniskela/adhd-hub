# ADHD Hub foundation plan (executed)

See the Cursor plan **ADHD Hub Foundation** for the full design. This repo now includes:

- First-class **projects** registry (`resolve_project`, `upsert_project`, UI sidebar)
- Dual capture: MCP skills + rules/hooks samples + indexer origins (`indexer`, `pending-reply`, `question`)
- Anti-nag digests (`remind_cooldown_days`, `digest_max_nudge`)
- Richer `/ui` (counts, streak, 14-day chart, progress snippets, forge)
- Per-project forge owner/repo/wiki_path overrides
- Installable skills under `skills/` via `npx skills add ./skills -g` (later `uniskela/adhd-hub`)

Saved for agents working in this tree; do not treat as a substitute for reading current code.
